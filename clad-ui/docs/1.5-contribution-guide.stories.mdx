import { Meta } from '@storybook/addon-docs/blocks';

<Meta
  title="Overview/Contributor Guide"
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

# Clad UI Development Guide

## Building new components quick start

TODO:

- Use `cx` instead of `classnames`
- Use `polished` for computed styles
- Write up sample component structure
  + When to use static class-based styles
  + When to use dynamic prop-based styles
- Write up component story file guide
