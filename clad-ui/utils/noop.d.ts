export default noop;
/**
 * A no-ops function used for default function props to avoid
 * re-creation of functions everytime a component is re-rendered
 *
 * @return {void} undefined
 */
declare function noop(): void;
