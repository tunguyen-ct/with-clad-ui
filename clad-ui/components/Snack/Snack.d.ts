export default Snack;
/**
 * Snack is used to inform user of a process that has just been performed or will perform.
 *
 * `🧪Beta`
 *
 * @type {React.FC<{
 *   children?: any,
 *   desktopSize?: string,
 *   onClose?: Function,
 *   autoHideDuration?: string,
 * }>}
 */
declare const Snack: React.FC<{
    children?: any;
    desktopSize?: string;
    onClose?: Function;
    autoHideDuration?: string;
}>;
import React from "react";
